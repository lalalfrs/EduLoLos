import 'dotenv/config';
import express from 'express';
import cookieParser from 'cookie-parser';
import mysql from 'mysql2/promise';
import bcrypt from 'bcryptjs';
import { randomBytes, randomUUID } from 'node:crypto';

const app = express();
const port = Number(process.env.API_PORT || 4000);
const pool = mysql.createPool({ host: process.env.MYSQL_HOST || '127.0.0.1', port: Number(process.env.MYSQL_PORT || 3306), user: process.env.MYSQL_USER || 'root', password: process.env.MYSQL_PASSWORD || '', database: process.env.MYSQL_DATABASE || 'edulolos', waitForConnections: true, connectionLimit: 10 });
const sessions = new Map<string, { userId: string; expiresAt: number }>();
app.use(express.json()); app.use(cookieParser());
app.use((_req, res, next) => { res.setHeader('Access-Control-Allow-Origin', process.env.APP_ORIGIN || 'http://localhost:3000'); res.setHeader('Access-Control-Allow-Credentials', 'true'); res.setHeader('Access-Control-Allow-Headers', 'Content-Type'); res.setHeader('Access-Control-Allow-Methods', 'GET,POST,PATCH,DELETE,OPTIONS'); if (_req.method === 'OPTIONS') return res.sendStatus(204); next(); });
const cookie = 'edulolos_session';
const getAuth = async (req: express.Request) => { const token = req.cookies[cookie]; if (!token) return null; const cached = sessions.get(token); if (cached && cached.expiresAt > Date.now()) return cached.userId; const [rows] = await pool.query<any[]>('SELECT user_id FROM sessions WHERE id = ? AND expires_at > NOW()', [token]); if (!rows[0]) return null; sessions.set(token, { userId: rows[0].user_id, expiresAt: Date.now() + 1000 * 60 * 60 * 24 * 7 }); return rows[0].user_id; };
const requireAuth = async (req: express.Request, res: express.Response) => { const userId = await getAuth(req); if (!userId) { res.status(401).json({ error: 'Unauthenticated' }); return null; } return userId; };
const startSession = async (res: express.Response, userId: string) => { const id = randomBytes(32).toString('hex'); await pool.query('INSERT INTO sessions (id,user_id,expires_at) VALUES (?,?,DATE_ADD(NOW(), INTERVAL 7 DAY))', [id, userId]); sessions.set(id, { userId, expiresAt: Date.now() + 604800000 }); res.cookie(cookie, id, { httpOnly: true, sameSite: 'lax', secure: process.env.NODE_ENV === 'production', maxAge: 604800000 }); };
app.post('/api/auth/signup', async (req, res) => { try { const { email, password, displayName = '' } = req.body; if (!email || !password || password.length < 6) return res.status(400).json({ error: 'Email dan password minimal 6 karakter wajib diisi.' }); const id = randomUUID(); await pool.query('INSERT INTO users (id,email,password_hash) VALUES (?,?,?)', [id, email.toLowerCase().trim(), await bcrypt.hash(password, 12)]); await pool.query('INSERT INTO profiles (id,display_name) VALUES (?,?)', [id, displayName]); await startSession(res, id); res.status(201).json({ user: { id, email } }); } catch (e: any) { res.status(e.code === 'ER_DUP_ENTRY' ? 409 : 500).json({ error: e.code === 'ER_DUP_ENTRY' ? 'Email sudah terdaftar.' : 'Pendaftaran gagal.' }); } });
app.post('/api/auth/signin', async (req, res) => { const [rows] = await pool.query<any[]>('SELECT id,email,password_hash FROM users WHERE email = ?', [String(req.body.email || '').toLowerCase().trim()]); if (!rows[0] || !(await bcrypt.compare(req.body.password || '', rows[0].password_hash))) return res.status(401).json({ error: 'Email atau password tidak valid.' }); await startSession(res, rows[0].id); res.json({ user: { id: rows[0].id, email: rows[0].email } }); });
app.post('/api/auth/signout', async (req, res) => { const token = req.cookies[cookie]; if (token) { await pool.query('DELETE FROM sessions WHERE id = ?', [token]); sessions.delete(token); } res.clearCookie(cookie); res.sendStatus(204); });
app.get('/api/auth/session', async (req, res) => { const userId = await getAuth(req); if (!userId) return res.json({ user: null }); const [rows] = await pool.query<any[]>('SELECT id,email FROM users WHERE id = ?', [userId]); res.json({ user: rows[0] || null }); });
app.get('/api/profiles/:id', async (req, res) => { const userId = await requireAuth(req, res); if (!userId || userId !== req.params.id) return; const [rows] = await pool.query('SELECT * FROM profiles WHERE id = ?', [userId]); res.json(rows[0] || null); });
app.patch('/api/profiles/:id', async (req, res) => { const userId = await requireAuth(req, res); if (!userId || userId !== req.params.id) return; const allowed = ['display_name','school','target_ptn','target_major','target_campus','onboarding_completed']; const fields = Object.keys(req.body).filter((key) => allowed.includes(key)); if (fields.length) await pool.query(`UPDATE profiles SET ${fields.map((f) => `${f} = ?`).join(', ')} WHERE id = ?`, [...fields.map((f) => req.body[f]), userId]); const [rows] = await pool.query('SELECT * FROM profiles WHERE id = ?', [userId]); res.json(rows[0]); });
const tables: Record<string, string> = { tasks: 'study_tasks', sessions: 'study_sessions', reminders: 'reminders', decks: 'flashcard_decks', cards: 'flashcards' };
app.get('/api/:resource', async (req, res) => { const userId = await requireAuth(req, res); const table = tables[req.params.resource]; if (!userId || !table) return res.status(404).json({ error: 'Not found' }); const [rows] = await pool.query(`SELECT * FROM ${table} WHERE user_id = ? ORDER BY created_at DESC`, [userId]); res.json(rows); });
app.post('/api/:resource', async (req, res) => { const userId = await requireAuth(req, res); const table = tables[req.params.resource]; if (!userId || !table) return res.status(404).json({ error: 'Not found' }); const body = { ...req.body, user_id: userId, id: randomUUID() }; const keys = Object.keys(body); await pool.query(`INSERT INTO ${table} (${keys.join(',')}) VALUES (${keys.map(() => '?').join(',')})`, keys.map((k) => body[k])); const [rows] = await pool.query(`SELECT * FROM ${table} WHERE id = ?`, [body.id]); res.status(201).json((rows as any[])[0]); });
app.patch('/api/:resource/:id', async (req, res) => { const userId = await requireAuth(req, res); const table = tables[req.params.resource]; if (!userId || !table) return res.status(404).json({ error: 'Not found' }); const keys = Object.keys(req.body); await pool.query(`UPDATE ${table} SET ${keys.map((k) => `${k} = ?`).join(', ')} WHERE id = ? AND user_id = ?`, [...keys.map((k) => req.body[k]), req.params.id, userId]); const [rows] = await pool.query(`SELECT * FROM ${table} WHERE id = ? AND user_id = ?`, [req.params.id, userId]); res.json((rows as any[])[0]); });
app.delete('/api/:resource/:id', async (req, res) => { const userId = await requireAuth(req, res); const table = tables[req.params.resource]; if (!userId || !table) return res.status(404).json({ error: 'Not found' }); await pool.query(`DELETE FROM ${table} WHERE id = ? AND user_id = ?`, [req.params.id, userId]); res.sendStatus(204); });
app.listen(port, () => console.log(`EduLoLos MySQL API listening on http://localhost:${port}`));
